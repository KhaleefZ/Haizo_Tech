{
  "name": "@haizo/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo '(e2e: run pnpm test:e2e with the apps running)'",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "echo '(e2e: nothing to lint yet)'"
  },
  "devDependencies": {
    "@haizo/config": "workspace:*",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
