import { SectionPlaceholder } from '../../../components/SectionPlaceholder';

export default function TestimonialsPage() {
  return (
    <SectionPlaceholder
      title="Testimonials"
      phase="Phase 4b"
      description="Add testimonials — a source URL and verification are required before one can be published."
    />
  );
}
