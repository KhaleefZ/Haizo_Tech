import { SectionPlaceholder } from '../../../components/SectionPlaceholder';

export default function AnnouncementsPage() {
  return (
    <SectionPlaceholder
      title="Announcements"
      phase="Phase 4c"
      description="Publish internal announcements to the team."
    />
  );
}
