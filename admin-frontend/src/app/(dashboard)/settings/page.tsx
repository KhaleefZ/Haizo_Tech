import { SectionPlaceholder } from '../../../components/SectionPlaceholder';

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      phase="Phase 4c"
      description="Account, notification preferences and system settings."
    />
  );
}
