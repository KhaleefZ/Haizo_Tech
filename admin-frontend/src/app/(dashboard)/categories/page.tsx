import { SectionPlaceholder } from '../../../components/SectionPlaceholder';

export default function CategoriesPage() {
  return (
    <SectionPlaceholder
      title="Categories"
      phase="Phase 4b"
      description="Order the work categories that drive the public filter."
    />
  );
}
