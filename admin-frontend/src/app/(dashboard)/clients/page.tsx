import { SectionPlaceholder } from '../../../components/SectionPlaceholder';

export default function ClientsPage() {
  return (
    <SectionPlaceholder
      title="Clients"
      phase="Phase 4c"
      description="Manage client organisations and their contacts."
    />
  );
}
